const num1 = 0;
const num2 = 0;
function getsum(num1, num2)
{
    // add two numbers
    const sum = num1 + num2;
    console.log(sum);
    return sum;
}

// display the sum
getsum(3,5);

module.exports = getsum;